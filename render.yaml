services:
  - type: web
    name: devos-backend
    runtime: docker
    rootDir: backend
    plan: free
    envVars:
      - key: PORT
        value: 10000
      - key: HEADLESS
        value: "true"
      - key: GEMINI_API_KEY
        sync: false
